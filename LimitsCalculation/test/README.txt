Before using this directory, it's required to set up links to directories called "condor" and "limits".

The recommended locations are "/data/users/USERNAME/condor/" and "/data/users/USERNAME/limits/".

You can create a soft link using, e.g.: "ln -s /data/users/USERNAME/condor/ condor"
